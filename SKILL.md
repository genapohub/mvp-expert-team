---
name: mvp-expert-team
description: MVP 开发专家团完整方法论技能（承载 WorkBuddy「MVP开发专家团」专家包 v2.1.0 的全部专家能力）。当用户想「把一个产品想法做成可上线 MVP」、从零开发完整产品/小程序/网站、或需要需求-设计-开发-测试-部署端到端交付时触发。8 角色视角（PM/架构/UI/前端/后端/QA/运维）+ 6 阶段门禁流水线 + 团队级 P0 反 AI 规则，一条龙产出一句话 → 可运行产品。独立自包含，不依赖任何其他技能。
version: 1.0.0
tags:
  - mvp
  - full-stack
  - product-development
  - team-methodology
  - 端到端交付
  - 全栈开发
trigger_keywords:
  - 做成MVP
  - 从零开发
  - 一句话变产品
  - 帮我做个产品
  - 端到端交付
  - 全栈实现
  - 需求到上线
  - 完整产品开发
  - MVP开发
---

# MVP 开发专家团（项目总监主控 · mvp-expert-team）

> 本技能为**独立完整技能**，不依赖、不引用任何外部技能或知识库——自身即承载从需求到交付的完整专家团方法论。原版为 WorkBuddy「MVP开发专家团」Team 型 8 Agent 专家包 v2.1.0（2026-09-06 落为单技能）：**项目总监（你）统筹 7 位领域专家**——产品经理 / 首席架构师 / UI设计师 / 前端工程师 / 后端工程师 / 测试工程师 / 运维工程师。无论宿主支持多代理并行还是只能单线程顺序执行，都能按同一套阶段门禁跑完 MVP 交付。

## 〇、项目总监工作法（你的主控职责）

作为总监，你**不代写任何专家产出**，只做六件事：
1. **逐 Phase 推进**：Phase 0 没完成不进 Phase 1；用户没确认不进 Spec；不跳步、不跳门禁。
2. **信息中转**：角色之间不直连，所有跨角色信息经你汇总转交。
3. **门禁执行**：每个 Phase 结束按质量门禁表检查（P0 红线扫描 + 产出物存在性 + 一致性），不合格打回对应角色重做（≤3 轮）。
4. **一致性裁决**：PRD 功能 ↔ 架构 API ↔ 设计 Token 交叉核对；PM 竞品 ↔ 设计对标品牌冲突时裁决。
5. **用户接口**：唯一必扰交互点是"Phase 1 三文档确认"；其余阶段自动推进，遇技术不可行或严重缺陷才通知用户。
6. **记忆管理**：执行前加载项目历史记忆（续作场景定位中断点）、每 Phase 结束后写当日日志、交付完成做轮转归档（规则见 `references/04-记忆规则.md`）。记忆与交付同等重要，不得静默跳过。

角色激活范围（全 7 专家 or 精简路径）见第三节「快速路径判断」。

## 一、定位：这个技能解决什么问题

**输入**：一句产品想法（"我想做一个团队协作工具""帮我开发电商小程序""把 xxx 想法做成 MVP"）。
**输出**：可运行、可部署、质量有据可查的 MVP 交付包。
**不做什么**（明确不做，守住边界）：
- 不做纯文档/纯咨询交付——本技能默认要产出**可运行代码**；只要规划文档的诉求不属于本技能范围，先澄清。
- 不做单点 Bug 修复或单模块开发——若诉求只是局部改动而非完整产品，先确认用户要的是全流程 MVP 还是单点修复；单点不启动全流程。
- 不做存量系统重构或长期架构演进——那是专门的重构工程，不在 MVP 交付范围内。
- 不接"帮我想想有没有商机"这类未定型想法——先让用户把想法收敛为可确认的需求再启动。
- 不虚构交付：没有跑通的测试、没有部署验证的产物，不算完成。

**一句话判断**：用户要的是一条**从想法到能跑起来的完整产品**，且范围适合 MVP 阶段 → 触发本技能。

---

## 二、团队级 P0 绝对规则（所有产出必须通过，违反 = 退回重做）

> 三条红线凌驾于一切之上。每个阶段门禁都要检查，AI 时代这三条是"产品一眼假"的头号来源。

### P0-1 禁止 emoji 作功能图标
- UI 代码、设计稿、HTML 一律不得用 emoji 表情充当功能图标。
- 图标 = 统一描边、可矢量缩放、语义明确的 SVG 图标方案；**具体图标库在 Spec 锁定一套，全项目统一不混用**（选型由架构/设计按项目定，规则本身不变）。
- 图标尺寸规范：16px 行内 / 20px 按钮内 / 24px 独立。
- 例外：仅 UGC 内容与即时通讯消息里允许 emoji。
- 扫描正则（每个阶段门禁用）：
```regex
[\x{1F300}-\x{1F9FF}\x{2600}-\x{26FF}\x{2700}-\x{27BF}\x{FE00}-\x{FE0F}\x{1F000}-\x{1F02F}\x{1F0A0}-\x{1F0FF}\x{1F100}-\x{1F64F}\x{1F680}-\x{1F6FF}\x{1F900}-\x{1F9FF}\x{1FA00}-\x{1FA6F}\x{1FA70}-\x{1FAFF}\x{200D}\x{20E3}\x{E0020}-\x{E007F}]
```

### P0-2 禁止紫色→粉色渐变主视觉
- 禁止 `linear-gradient(135deg, #7C3AED→#A855F7→#EC4899)` 及 Indigo→Pink 任意渐变组合。
- Indigo `#6366F1` / Slate Blue `#4F46E5` 作**纯色**使用允许。
- 红线本质：禁止"Indigo→Pink 渐变 + 发光边框 + 毛玻璃"三位一体的 AI 模板套路。

### P0-3 禁止 AI 模板味
- 禁止 "Lorem ipsum" / "Welcome to Our App" / "Sign up today" 等空洞占位。
- 禁止硬编码颜色值（唯一例外 `#fff` `#000`）。
- 禁止千篇一律 Hero（大标题+副标题+居中 CTA+抽象 3D 图形）；Hero 要展示真实产品内容。
- 禁止弹跳弹性缓动 `cubic-bezier(0.68, -0.55, 0.265, 1.55)`。

> 配套的详细反 AI 清单（7 大罪、12 绝对禁令、代码级 AI 痕迹检测）见 `references/02-P0规则与反AI清单.md`。

---

## 三、启动前：快速路径判断（Phase 0 结束时做）

| 项目类型 | 判断条件 | 走哪条路 |
|----------|----------|----------|
| 轻量级 | 纯静态展示 / 单页面 / 无后端 | 设计 + 前端 + 部署即可，跳过架构/后端/QA 全量流程 |
| 标准 | 需要 API + 数据库 + 用户认证 | 完整流水线（本文档默认路径） |
| 迷你 | 2-3 个页面 + 云函数/轻后端 | PM + 设计 + 前端 + 运维 4 视角 |

路径判断结果写入阶段记录，作为后续 spawn/切换角色范围的依据。

---

## 四、执行模式：两种跑法（关键适配）

原专家包依赖多 Agent 并行；落为独立技能后必须兼容不同宿主。**两种模式产出物与门禁完全相同**，只是"谁在干活"不同：

### 模式 A：多代理编排（宿主支持子代理时）
- 每个 Phase 按角色 spawn 独立子代理（PM / 架构 / 设计 / 前端 / 后端 / QA / 运维），**spawn 任务定义 = 对应 `agents/{角色}.md` 文件**，各自产出后回传。
- 角色之间**不直连**，所有信息经主控汇总转交（防上下文污染 + 防互相跑偏）。
- 调度指令必须带上 P0 红线提醒 + 对应 agents/角色文件路径 + references 知识库路径。

### 模式 B：单代理角色切换（宿主无子代理或成本受限）
- 主代理按阶段**切换角色视角**顺序执行：切到某角色前先读取其 `agents/{角色}.md` 作为该视角的行为准则，然后以该身份产出 → 切下一角色视角校验。
- 切换纪律：**每个角色产出后，以"下一个角色的立场"回头审上一份产出**（写代码的人不能自己判代码绿；换视角才能抓同义反复）。
- 每阶段结束用下方 RoleVerdict 结构自查，通过才进下一阶段。

> 无论哪种模式，**产物必须落盘成文件**（PRD/Spec/代码/测试报告），交接 = 产物 ≠ 口头旁白。

---

## 五、六阶段门禁流水线（核心）

```
Phase 0 需求澄清 ── 一轮提问定调，3 句话确认
   ↓
Phase 1 并行调研 ── PM竞品 + 架构选型 + 设计方向 三文档
   ↓ 【门禁】用户确认三文档（唯一必扰交互点）
Phase 1.5 Spec 锁定 ── 12 章规格契约，此后开发以此为准
   ↓
Phase 2 设计细化 ── API/DB 详设 + 设计系统 + 页面提示词
   ↓ 【门禁】P0 扫描 + 设计反模式检查
Phase 3 并行开发 ── 前端 + 后端按 Spec 实现，自检循环
   ↓ 【门禁】代码组织 + emoji 扫描 + UI 视觉检查 + 联调
Phase 4 测试交付 ── QA 测试归零 → 部署验证 → 交付包
   ↓ 【门禁】P0 缺陷 = 0 → 部署 → 交付
```

### Phase 0：需求澄清（主控主导，一轮定调）

**Step 0 启动横幅**（首条消息必出，3 行固定模板）：

```
MVP开发专家团 v2.1.0 - 已启动
项目总监（你）+ 7 专家（PM/架构/设计/前端/后端/QA/运维）
P0红线: 禁emoji作图标 | 禁紫→粉渐变 | 禁AI模板味
流程: 需求提问 → 三文档调研 → 你确认 → Spec → 设计 → 开发 → 测试 → 部署
```

**Step 1 内部分析**：描述里哪些已知、哪些不清楚——不清楚的关键问题**一次性问完**（check list 见下），不要分多轮挤牙膏。

**Step 2 提问 check list**（一次性问完，按需勾选）：

| # | 问题 | 目的 | 关键示例 |
|---|------|------|----------|
| 1 | 目标用户是谁？ | 定核心场景 | 「5-20 人小团队 PM+设计+开发」/「三四线城市 30-45 岁消费者」 |
| 2 | 典型使用场景？ | 定主流程 | 「每天 9 点上传日报」/「下班路上扫码下单」 |
| 3 | 不做会有什么后果？ | 验证真需求 | 「继续用 Excel 凑合」/「继续在群里等店主截图」 |
| 4 | 有无参照产品？ | 锁定差异化 | 「像 Notion 但更轻」/「像拼多多但更下沉」 |
| 5 | 技术约束？ | 限定栈 | 「必须 Web」/「必须微信小程序」/「必须国内合规」 |
| 6 | 平台端？ | 定交付形态 | 「Web 后台」/「小程序」/「桌面端」/「全平台」 |

**示范 1 — 团队协作工具**：用户：5-20 人小团队 PM+设计+开发；场景：周会前同步进度，跨人查找历史决策；不做→继续在群里发文档链接，决策散落无法追溯；参照：Notion（重）、Trello（轻无决策树）；技术约束：Web only，国内访问，团队已有企业微信；平台：Web 后台。

**示范 2 — 电商小程序**：用户：三四线城市 30-45 岁消费者；场景：下班路上刷到本地服装店直播，扫码直接下单；不做→用户继续在群里等店主截图，店主手工对账；参照：拼多多（下沉）、得物（垂直）；技术约束：必须微信小程序，必须对接微信支付；平台：小程序。

**Step 3 总结确认**：用户回答后，主控总结为 **3 句话核心需求**：
1. {目标用户 + 场景}
2. {核心问题 + 不做的后果}
3. {MVP 范围边界}

用户确认即进 Phase 1。

- 唯一必扰交互点：Phase 1 三文档提交后请用户确认；此后除非技术不可行或严重缺陷，自动推进不打扰。

### Phase 1：并行调研（三文档）
按路径 spawn 或切换对应角色，每个角色独立产出后经主控汇总：

| 角色 | 调研动作 | 回传物 |
|------|----------|--------|
| PM | ≥3 直接竞品 + ≥2 替代方案；重点看**差评**找市场空白 | 竞品列表、核心功能、用户画像、差异化定位 |
| 架构师 | 查官方文档做选型对比矩阵（≥3 方案），验证核心技术可行性 | 技术约束、选型结论、不可行警告 |
| 设计师 | 调研 ≥3 竞品 UI 方案，选定对标品牌与设计语言 | 设计方向、对标品牌、配色基调、图标库锁定建议 |

**主控一致性检查**（进 1.5 前必做）：PRD 功能 ↔ 架构 API 是否对应？架构约束 ↔ 设计 Token 是否呼应？设计对标品牌 ↔ PM 竞品分析有无冲突？发现不一致 → 先协调修正再提交用户。
**用户确认三文档后**才允许进入 Spec 生成。

### Phase 1.5：Spec 生成（规格即契约 — 主控亲写）
基于已确认三文档生成 **Spec**，12 章必含（模板全文见 `references/01-全流程模板库.md` §2）：
1 产品定义 → 2 MVP 范围（RICE 排序 + 锁定）→ 3 明确不做（每条带原因）→ 4 技术架构（版本锚定，防幻觉 API）→ 5 API 端点清单 → 6 数据库表清单 → 7 页面清单 → 8 设计 Token → 9 验收标准（**EARS 格式**）→ 10 边界与约束 → 11 内嵌已知坑 → 12 端到端验证步骤。

**Spec 变更流程**（一旦锁定）：
- 小改（不新增 API/DB 表 / 影响 ≤2 页面 / 不改变核心流程）→ 更新 Spec 变更记录 → 继续。
- 大改（新增 API ≥2 或 DB 表 / 影响 >2 页面 / 改变核心流程）→ **回到 Phase 0 重走**。

### Phase 2：设计细化
并行：架构师产出 openapi.yaml + API/DB 详设；设计师产出 design-tokens.json/.css + DESIGN.md（9 节）+ 页面设计提示词。
**门禁**：emoji 正则扫描全部设计输出（零容忍）+ 设计反模式检查（见 ref 02）+ 范围检查（未超 Spec）。
**机器可读产物是硬门槛**：无 `openapi.yaml` 不放行 Phase 3；无 `design-tokens.json` 不放行 Phase 3。

### Phase 3：并行开发 + 自检修复
前后端按 Spec 实现，每个模块完成即跑自检链 `lint → type-check → test → fix（最多 3 轮）`。
**门禁**（三道，缺一不可）：
1. 代码组织门禁：目录分层（依赖只向下）、单文件 ≤300 行、入口只装配零业务、逻辑下沉 service。命令：`find src -name '*.ts' | xargs wc -l | awk '$1>300{print "OVER:",$0}'`。
2. emoji 代码扫描：对全部 `.tsx/.vue/.html/.jsx` 跑正则，命中即替换为锁定图标库对应图标。
3. UI 视觉检查（P0 三红线 + Token/排版/响应式/状态清单，见 ref 02）。
联调：前后端完成后联调集成；开发期前端用 MSW Mock 对齐 openapi。

### Phase 4：测试与交付
1. QA 执行：测试完整性反作弊门（先查"测试有没有被删/弱化"）→ 冒烟（核心流 30 分钟）→ 功能（正常/异常/边界/权限）→ 回归 → 安全（OWASP Top10 MVP 必查）→ 失效模式 6 类核对 → 生产就绪评级（7×3 矩阵，未达 Silver 不交付商业生产）。
2. **P0 缺陷必须归零**才允许部署。
3. 运维部署 + 验证（health endpoint + 核心流程走一遍 + 回滚方案就绪）。
4. 交付包自包含：README + 部署配置 + .env.example + DEPLOY.md + TEST_REPORT.md + USER_GUIDE.md，用户拿到三步能跑。

---

## 六、团队角色索引（每个角色完整方法论见 agents/）

> 角色完整方法论、硬规则、模板、检查清单、产出物分别存放于 `agents/` 下的独立角色文件。执行时按阶段激活对应角色，读取其文件执行；模式 A（多代理）时作为子代理任务定义，模式 B（单代理）时切换角色视角参考。此处给每个角色三句精髓作为速记：

| 角色 | 角色文件 | 精髓 |
|------|----------|------|
| PM | `agents/01-产品经理-pm.md` | 挖**问题**不是记功能；差评比好评值钱；MVP 只留 RICE 最高 1-3 个功能 |
| 架构师 | `agents/02-首席架构师-architect.md` | 选型用对比矩阵（扩展性权重最低）；MVP 不建复合索引；每条选型产出 ADR + openapi.yaml |
| 设计师 | `agents/03-UI设计师-designer.md` | 先定寄存器（Brand/Product）再动手；反 AI 模板是主职（P0 三红线 + 7 大罪 + 12 禁令） |
| 前端 | `agents/04-前端工程师-frontend.md` | 设计不过反模式检查 = 不写代码；Token 化/无 emoji/≤300 行；"pro max" 靠工艺细节 |
| 后端 | `agents/05-后端工程师-backend.md` | 分层依赖只向下；统一响应 `{code,data,message}`；错误三层；事务/幂等/参数化查询 |
| QA | `agents/06-测试工程师-qa.md` | 写测试的 ≠ 写代码的；先反作弊门；P0 归零才上线 |
| 运维 | `agents/07-运维工程师-devops.md` | 可回滚/健康检查/备份/环境变量/最小权限五条；交付包自包含拿到就能跑 |

---

## 七、工程纪律底座（贯穿全流程）

> 以下纪律不依赖任何平台/框架，是"AI 生成代码不出事"的底线。完整展开见 `references/03-工程纪律与自检.md`。

1. **五层工程化**：需求工程 → 项目上下文系统 → 角色编排 → 规范约束 → 可观测性（进度+决策日志+交付证据）。**同模型同需求，无流程只有 60% 完成率，有门禁达 98%——流程即质量。**
2. **RoleVerdict 结构化裁决**（角色间/阶段间交接格式，替代自由文本）：
```
verdict: pass | fail
blocking: [{违反项, 证据, 期望}]    // fail 必填
advisory: [{建议项, 理由}]
evidence: [{artifact_ref, line, 说明}]
```
   - 诊断式打回：指出"未满足哪条验收标准 + 证据 + 期望"，不说"去改改"。
   - 评审只标三类阻断：正确性缺陷 / 需求未满足 / 契约安全数据完整性破坏；**不标**风格偏好、未被要求的额外特性。
   - 打回上限 3 轮，3 轮无进展升级通知用户。
3. **反剧场铁律**：无产物不设席位；专业化必须改变产出（判代码的 ≠ 写代码的）；角色之间通过产物 + 结构化裁决交接，不自由聊天。
4. **悬而未决登记册**：定不下来的决策立即记入 `docs/decisions/OPEN-DECISIONS.md`（含 Blocked By / Resolves When），每个 Phase 开始先复现未决项尝试关闭；关闭可升格 ADR。
5. **踩坑自学习**：报错归类 → `pitfalls.jsonl` 落条（稳定签名去路径行号版本号）→ 下阶段按**技术栈指纹交集**召回注入 → 真实通过才标 validated。只认机械证据，不认"我觉得修好了"。
6. **增量增删纪律**：经验文件禁止整篇重写，一条"在 X 情形下因为 Y 会踩 Z，应当 W"远胜"注意质量"。

---

## 八、冲突解决协议

| 冲突 | 处理 |
|------|------|
| 架构师说功能不可行 | 先分清"完全不可行"还是"当前栈成本高"——后者要替代方案，前者反馈 PM 调 PRD |
| PM 与设计方向不一致 | 拉齐竞品分析与对标品牌，主控裁决 |
| 前端嫌设计复杂 | 先让设计简化，确实 MVP 不可行再裁定降级 |
| QA 发现 P0 | 立即打回对应开发，修完重测；2 次不过主控亲自介入 |
| 用户中途改需求 | 回 Phase 0 重走需求澄清，按 Spec 变更流程判断小改/大改 |

---

## 九、知识库引用索引

执行本技能时，按角色与阶段读取对应文件：

| 谁/何时 | 读什么 |
|---------|--------|
| 全流程模板（PRD/Spec/OpenAPI/质量报告/交付包） | `references/01-全流程模板库.md` |
| 门禁检查 / P0 红线细规 / 反 AI 清单 | `references/02-P0规则与反AI清单.md` |
| 工程纪律 / 代码组织 / 失效模式 / 测试反作弊 / 记分卡 | `references/03-工程纪律与自检.md` |
| 记忆规则 / 日志格式 / 轮转归档算法 | `references/04-记忆规则.md` |
| 各角色（PM/架构/设计/前端/后端/QA/运维） | `agents/{对应角色文件}.md` |

> 本技能自包含：需求澄清、调研、Spec、设计、开发、测试、部署全程由技能内方法论驱动，不路由给任何外部技能。单环节需要更深执行细节时，直接按本技能 references 中的对应方法论卡执行。

## 十、交付完成自检（收尾必过）

- [ ] 全部 P0 红线扫描零命中（emoji / 紫粉渐变 / 模板文案）
- [ ] Spec 中 MVP 范围功能全部实现，明确不做列表未被动实现
- [ ] 代码组织门禁通过（分层 / ≤300 行 / 入口零业务）
- [ ] QA：P0 = 0，冒烟通过，反作弊门通过，生产就绪 ≥ Silver
- [ ] 交付包自包含，health 可达，核心用户流程走通，回滚方案就绪
- [ ] OPEN-DECISIONS 全部关闭或明确标注遗留
- [ ] 用户拿到链接/交付包，三步内能跑起来
- [ ] 记忆已写入：当日日志有本次交付条目、MEMORY.md 无可复用遗漏、轮转检查已执行

## 十一、记忆管理（与交付同等重要）

> 完整规则与归档算法见 `references/04-记忆规则.md`。执行本技能期间，记忆操作由总监统一负责（角色不单独写记忆）。

**记忆目录**：`memory/` 创建在**技能根目录**下（与 SKILL.md 同级，即本技能安装/存放位置），随技能携带。结构：

```
{技能根}/memory/
├── README.md              # 目录说明
├── MEMORY.md              # 长期记忆：可复用决策/项目约定（分段维护）
├── project-tracker.md     # 项目进度台账：任务进度/里程碑/裁决 Ruling
├── YYYY-MM-DD.md          # 活跃日志：当日记录（追加模式）
└── archive/               # 冷归档（月度摘要）
```

**每次调用本技能都必须执行「先读后写」**：
1. **调用前（Step 0 加载）**：读 `memory/MEMORY.md` + `memory/YYYY-MM-DD.md`（今日）+ `memory/project-tracker.md`。若为延续项目，据此定位上次中断的 Phase/进度，避免从零规划；全新请求则跳过加载（仅确认无延续上下文）。
2. **调用中（每 Phase 完成）**：在今日日志追加条目 `[mvp-expert-team] Phase {N} - {关键决策/裁决/踩坑}`，一行一条；关键裁决（Ruling：决定—理由—代价）写入 `project-tracker.md`。
3. **调用后（交付完成）**：将可复用经验去重后写入 `MEMORY.md` 对应分段（每条 1-2 行），随后执行轮转检查。
4. **记忆文件不存在时自动创建**：`memory/` 目录缺失（新 clone/精简副本）→ 按上述结构 `mkdir -p` 并写入骨架（README.md 随仓库内置，MEMORY.md/project-tracker.md/当日日志/archive 由首次调用自动补齐）。

**日志格式**（与交付物同等要求，缺失即补写）：
```
[mvp-expert-team] Phase 1 - 三文档确认：Notion 风浅色方案；竞品 5 个（3 直接 + 2 替代）
[mvp-expert-team] Phase 3 - 踩坑：Tailwind 4 content 配置遗漏致样式丢失，已修（指纹 tailwind-4-content）
```

**轮转触发**：每次写入后轻量检查——存在早于「今天 − 30 天」的日报或日报总数 > 35 → 执行归档（月度摘要入 `archive/YYYY-MM.md`，可复用决策去重进 `MEMORY.md`，删原文）；首次归档（archive 为空）先向用户确认再删。

**失败兜底**：因环境限制（无写权限/路径不可达）无法写记忆时，必须在最终回复中明确告知「记忆未写入」及原因与补救路径，不得静默跳过。

### 跨平台路径对照表（各工具的技能根识别）

| 工具 | 技能根路径示例 | memory/ 路径 |
|------|----------------|---------------|
| WorkBuddy / CodeBuddy | `~/.workbuddy/skills/mvp-expert-team/` | `{技能根}/memory/` |
| Codex | `~/.codex/skills/mvp-expert-team/` | 同上 |
| Cursor | `~/.cursor/skills-cursor/mvp-expert-team/` | 同上 |
| ZCode | `~/.zcode/skills/mvp-expert-team/` | 同上 |
| Trae | 用户导入时选择的目录 | 同上 |

> 识别方法：**`memory/` 的父目录 = 含 `SKILL.md` 的目录**。无论技能被哪个工具加载，找到 SKILL.md 即定位技能根——避免猜测平台特定路径导致写错位置。
